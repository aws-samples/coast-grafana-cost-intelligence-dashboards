groups:
  - name: apiserver-alerts
    rules:
      - alert: "API Server request drop" 
        expr: rate(apiserver_flowcontrol_request_concurrency_in_use[1m]) > 0
        for: 1s
        labels:
          severity: critical
        annotations:
          summary: API server is receiving too many request
